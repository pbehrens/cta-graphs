#R_HOME=$(shell R RHOME)

PYTHON_INCLUDES=@PYTHON_INCLUDE_DIR@

PIC_FLAGS=-fPIC

CFLAGS=$(PIC_FLAGS) -g -I$(PYTHON_INCLUDES) -I$(R_HOME)/include
OBJS=$(shell find src -name '*.o')

lib RSInternal.so: $(OBJS)
	$(CC) -shared -o @R_LIBRARY_DIR@/libs/RSInternal.so $(OBJS) -lR -L$(R_HOME)/lib
